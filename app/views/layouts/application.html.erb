<!DOCTYPE html>
<html>
  <head>
    <title>Przyjaciele</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= turbo_include_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= hotwire_livereload_tags if Rails.env.development? %>
  </head>

  <body class="container">
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
        <p class="navbar-brand">Friends</p>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <%= link_to "All", root_path, class: ["nav-link", ("active" if current_page?(root_path))] %>
            <%= link_to "School", school_friends_path, class: ["nav-link", ("active" if current_page?(school_friends_path))] %>
            <%= link_to "Work", work_friends_path, class: ["nav-link", ("active" if current_page?(work_friends_path))] %>
            <%= link_to "Holiday", holiday_friends_path, class: ["nav-link", ("active" if current_page?(holiday_friends_path))] %>
            <%= link_to "Party", party_friends_path, class: ["nav-link", ("active" if current_page?(party_friends_path))] %>
          </div>
        </div>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
